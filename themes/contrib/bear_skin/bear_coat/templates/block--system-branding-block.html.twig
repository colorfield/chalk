{% extends "block.html.twig" %}
{% block content %}
  {% if site_logo %}
    <div class="site-header__logo">
      <a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home">
        <img src="{{ site_logo }}" alt="{{ 'Home'|t }}" />
      </a>
    </div>
  {% endif %}
  {% if site_name %}
    <div class="site-header__name">
      <h1 title="{{ 'Home'|t }}" rel="home">{{ site_name }}</h1>
    </div>
  {% endif %}
  {% if site_slogan %}
    <div class="site-header__name">
      <h2> {{ site_slogan }}</h2>
    </div>
  {% endif %}
{% endblock %}
